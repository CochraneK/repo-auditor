# Long Gate 项目审计

> 审计日期：2026-09-16  
> 审计对象：`CochraneK/long-gate`  
> 审计基线：`main` @ `7657ec0a0b94fe6a8a91639940273fd06412ee8c`  
> Portfolio 决策：**CONTINUE · 94 · P0-NOW**  
> 注意：94 是“现在值得投入”的优先级，不是质量分。

## 一句话结论

**Long Gate 已经达到很强的 pre-1.0 engineering baseline；当前最重要的不是继续加功能，而是把供应链、GitHub profile、security-boundary CI 语义和发布可复现性收口。**

## 多维质量画像

> 质量维度独立于 Portfolio priority；不计算单一总分。

| 维度 | 分数 |
|---|---:|
| Purpose & scope | **5/5** |
| Correctness | **4/5** |
| Security & privacy | **5/5** |
| Supply chain | **5/5** |
| Reproducibility | **4/5** |
| Release engineering | **3/5** |
| Documentation & onboarding | **5/5** |
| Maintainability | **4/5** |
| Community surface | **3/5** |

审计基线观察到：

- **6 / 6 GitHub quality gates 全绿**：
  - test
  - security-boundary
  - benchmark-smoke
  - release-readiness
  - security-audit
  - CodeQL
- 普通测试日志：**58 passed**
- raw/private 与 network capability 分离已经同时出现在：
  - 架构文档；
  - security invariants；
  - hardened compose；
  - deployment contract tests；
- Model Vault 已做到：
  - curated catalog；
  - upstream immutable revision pin；
  - SHA-256 verification；
  - setup worker 与 private worker 能力分离；
- semantic preview 仍固定 `release_allowed=false`，没有把“LLM 改写”宣传成匿名化。

## P0 · 阻断项

**本次没有发现阻断继续开发的 P0。**

这不等于“已经获得生产隐私认证”。项目自己对这一点的 non-claims 写得比较清楚，应继续保持。

## 2026-09-16 remediation recheck

GO remediation 已完成并验证三项自动整改：

- **LG-P1-002 · FIXED**：第三方 GitHub Actions 使用 immutable commit SHA；
- **LG-P1-003 · FIXED**：`security-boundary` 直接覆盖关键 capability-boundary tests；
- **LG-P2-001 · FIXED**：CI/dev 使用 `constraints-ci.txt` 控制顶层依赖漂移，同时保留 library-friendly runtime ranges；
- 新 `main` @ `7657ec0a0b94fe6a8a91639940273fd06412ee8c` 上 **6 / 6 quality gates 全绿**。

当前开放 P1 只剩 GitHub repository profile metadata，且它属于 external-blocked，而不是可自动整改项。

## P1 · 高优先收口

### 1. GitHub 仓库 Profile 还没有真正配置

GitHub API 当前返回：

- `description = null`
- `homepage = null`
- `topics = []`

但仓库已经有 `docs/repository-profile.md`。

这意味着“想怎么展示”已经写好了，“GitHub 搜索/主页实际上怎么看到”还没落地。

**建议：**把 description、Safe Demo homepage、topics 真正配置到 GitHub repository profile。

---

### 2. GitHub Actions 供应链还没有达到 Model Vault 同等级标准 · **FIXED**

当前 workflows 大量使用：

- `actions/checkout@v4`
- `actions/setup-python@v5`
- `github/codeql-action/...@v3`
- `actions/upload-artifact@v4`
- `aquasecurity/trivy-action@v0.36.0`
- `anchore/sbom-action@v0.17.2`

这些是 mutable tag。

而 Model Vault 已经使用：

> immutable upstream revision + SHA-256

安全工具自身的 CI supply chain 最好统一标准。

**建议：**第三方 Actions 全部 pin 到 reviewed commit SHA，并在旁边保留版本注释。

---

### 3. `security-boundary` badge 的实际测试范围比名字窄 · **FIXED**

`.github/workflows/security.yml` 当前显式运行：

- `tests/test_policy.py`
- `tests/test_pipeline.py`

但真正验证：

- private worker 有 raw + no network；
- model setup 有 network + no private mount；
- cloud worker 只有 safe mount；

的是 `tests/test_deployment_contract.py`。

它现在由 general test workflow 覆盖，而不是 security-boundary workflow。

**建议：**把 deployment contract、Model Vault boundary 等直接加入 security-boundary workflow，让 badge 的语义与真实 claim 对齐。

## P2 · 重要但不阻断

### 4. CI / dev 环境缺少锁定策略 · **FIXED**

新增 `constraints-ci.txt`，CI/dev 的顶层 runtime、dev、安全审计与构建工具链现在使用统一的受审查约束；`pyproject.toml` 继续保留适合 library 的宽松 runtime ranges。

整改过程中 `pip-audit` 实际发现旧版 pytest 的已知漏洞，约束版本已升级；合并后的 exact-main 六个质量门全部通过。

这是一套 constraints strategy，而不是所有传递依赖都带 hash 的完全 lock，因此 Reproducibility 从 **3/5 → 4/5**。
---

### 5. Tag build 有 artifact，但还没有 release attestation/signing

`release.yml` 当前负责：

- build
- upload artifact

这已经是好基础，但还没做到：

- provenance/attestation；
- signature；
- 明确 GitHub Release / PyPI 发布策略。

在 pre-1.0 阶段完全可以接受；正式发布前补。

---

### 6. Branch protection / rulesets 本次无法验证

这是**未验证**，不是“确认没有”。

当前 GitHub connector 没有 repository administration 权限，不能据此判断 protected main / required checks。

正式 release-hardening 前人工确认一次即可。

## 已经做得很好的地方

Long Gate 当前最强的部分不是“功能多”，而是**边界逐渐变成可执行约束**：

1. capability separation 比 prompt guardrail 更可信；
2. raw / pseudonymized row-level data 默认禁止出关；
3. synthetic 不等于 exact inference，真实统计可以本地算；
4. semantic transformation 没被包装成匿名化；
5. public demo 不部署 private worker；
6. Model Setup 与 Private Processing 拆成不同能力集；
7. model download 既 pin upstream revision，又验 SHA-256；
8. Trust Report / provenance / benchmark / non-claims 都比较克制；
9. Release Readiness 已做 clean-venv wheel install；
10. 项目文档面已经具备明星仓库基础。

## 下一阶段建议

不要继续横向扩功能。

按顺序收口：

1. GitHub Profile metadata；
2. release attestation / first-release policy；
3. 再决定何时进入真正的 semantic privacy attack research。

其中 Action pinning、security-boundary 扩展与 CI/dev constraints strategy 均已完成。

## Repo-auditor 决策

**CONTINUE · 94 · P0-NOW**

原因不是“项目只有 6 分没做完”，而是：

> **核心架构已经成立，当前有一批非常明确、投入产出比高的收口项。**

做完这些后，Long Gate 应从“持续开发模式”切到“稳定迭代 + adversarial research”模式。
